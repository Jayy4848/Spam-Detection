[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && gunicorn sms_security.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
